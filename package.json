{
  "name": "css",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "node-sass style.scss style.css -w",
    "prefix:css": "postcss --use autoprefixer -b 'last 10 versions' styleor.css -o style.prefix.css",
    "compress:css": "node-sass style.prefix.css style.css --output-style compressed"
  },
  "author": "Kunal",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "autoprefixer": "^9.8.0",
    "node-sass": "^4.14.0",
    "postcss-cli": "^7.1.1"
  }
}
